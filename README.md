# PRODUCT HUB

Plastic products factory catalog — MVP demo with mock data.

```bash
npm install
npm run dev
```

Open `http://localhost:5173`

- Storefront: `/` `/products` `/products/:sku` `/categories` `/new` `/contact` `/quote`
- Admin: `/admin`
